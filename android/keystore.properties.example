# Example keystore.properties for android signing.
# Do NOT commit your real keystore or passwords. Use GitHub Actions secrets or create a local file ignored by git.

# Example content (replace with your values or set via CI secrets):
storeFile=keystore.jks
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
